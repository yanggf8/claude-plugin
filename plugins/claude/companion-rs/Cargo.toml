[package]
name = "claude-companion"
version = "0.1.0"
edition = "2024"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
# Unix-only: `setsid(2)` in child `pre_exec` to detach background spawns (Node `detached: true`).
libc = "0.2"
